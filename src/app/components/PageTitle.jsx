import React from 'react'

const PageTitle = (props) => {
  return (
    <div>
      <h2 className="py-10 text-4xl font-bold text-gray-700">{props.title}</h2>
    </div>
  )
}

export default PageTitle
